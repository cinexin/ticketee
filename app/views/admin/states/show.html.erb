<h2><%= @state.name %></h2>

<div id ="state_color_info">
	<p>Foreground color: <%= @state.color %><br />
	Background color: <%= @state.background %></p>
</div>

<%= link_to "Edit State", edit_admin_state_path(@state) %>
<%= link_to "Delete State", admin_state_path(@state), method: :delete, data: {confirm: "Are you sure you want to delete this state?"} %>